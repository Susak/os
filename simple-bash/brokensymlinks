#!/bin/bash

f() {
    for cur_dir in "$@"
    do
        if [[ -d "$cur_dir" ]] 
        then
            f "$cur_dir"/*
        fi
        if [[ ! -e "$cur_dir" && ! -f "$cur_dir" ]]
        then
            echo "$cur_dir"
        fi 
    done
}

f "$@"
